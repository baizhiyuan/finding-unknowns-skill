---
description: "Run a blind-spot pass — reconnaissance that surfaces your unknown unknowns before work starts."
---

# blindspot

Direct entry into Technique 1 (blind-spot pass).

## Dispatch

1. Read `skills/finding-unknowns/SKILL.md` from this plugin (or
   `~/.claude/skills/finding-unknowns/SKILL.md` for manual installs).
2. Run **Technique 1 — Blind-spot pass** exactly as specified: establish goal and
   experience level, explore the territory before asking anything, and report in the five
   standard sections (Landmines, Hidden context, What good looks like, Questions you
   should be asking, Rewritten request). Delegate to the `blindspot-scout` agent when a
   real codebase or domain sweep is involved.
3. Treat the user's arguments as the goal/area to probe:

```text
$ARGUMENTS
```

Discovery ends at understanding — never continue into implementation from this command.
